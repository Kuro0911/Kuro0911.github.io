import styled from "styled-components";

const Wrapper = styled.div`
  .wrap {
    width: 70vw;
    height: 60vh;
  }
  .heading {
    font-size: xxx-large;
    color: white;
  }
`;
export default Wrapper;
